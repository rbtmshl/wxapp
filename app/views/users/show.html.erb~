<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    <section>
      <%= render 'shared/stats' %>
    </section>  
  </aside>
  <div class="span8">
  <%= render 'follow_form' if signed_in? %>
    <% if @user.forecasts.any? %>
      <h3>Forecasts (<%= @user.forecasts.count %>)</h3>
      <ol class="forecasts">
        <%= render @forecasts %>
      </ol>
      <%-# <%= will_paginate @microposts -%> 
    <% end %>    
  </div>
</div>


