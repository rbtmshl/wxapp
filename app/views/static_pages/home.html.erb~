<h1><script language="Javascript">document.write(geoplugin_city()+", "+geoplugin_region());</script>: <%= h(@temperature) %></h1>

<% if signed_in? %>
  <div class="row">
    <aside class="span4">
      <section class="userinfostuff">
        <%= render 'shared/user_info' %>
        <%= render 'shared/stats' %>
      </section>
      <section class="formy">
        <%= render 'shared/forecast_form' %>
      </section>
      <section class="formy">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="span6">
      <%= render 'shared/toptab' %>
    </div>
  </div>
<% else %>
  <div class="center hero-unit">
    <h4>Welcome to the Weather App</h4>
    <h2>
      This is the home page for the Weather App!
    </h2>
    <%= link_to "Sign up now!", signup_path, 
                                class: "btn btn-large btn-primary" %>
  </div>

<% end %> 

